{
  "mcpServers": {
    "ida": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--exclude-newer=1s",
        "--project",
        "${PLUGIN_ROOT}",
        "ida-mcp",
        "stdio",
        "--agent=copilot"
      ],
      "env": {
        "IDA_MCP_ID": "${IDA_MCP_ID:-}",
        "IDA_MCP_IDLE_TIMEOUT": "${IDA_MCP_IDLE_TIMEOUT:-}",
        "IDAUSR": "${IDAUSR:-}",
        "IDA_NEXUS_STATE_DIR": "${IDA_NEXUS_STATE_DIR:-}"
      },
      "tools": [
        "*"
      ]
    }
  }
}
