{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__(.*[_:])?ida__.*",
        "hooks": [
          {
            "type": "command",
            "command": "uv run --exclude-newer=1s --project ${PLUGIN_ROOT} ida-mcp --report-session=codex",
            "statusMessage": "Linking IDA trace to Codex session"
          }
        ]
      }
    ]
  }
}
